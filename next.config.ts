import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Site institucional estático/ISR. Usa <img> puro, sem next/image,
  // então não precisa configurar remotePatterns.
};

export default nextConfig;
